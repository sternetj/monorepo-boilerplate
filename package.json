{
  "private": true,
  "scripts": {
    "dev": "cd packages/shell && yarn dev",
    "build": "lerna run build --scope={@monorepo/shell}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "3.10.6"
  }
}
